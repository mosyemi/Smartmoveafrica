import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand
  static const primary    = Color(0xFF0D47A1); // Deep Africa Blue
  static const secondary  = Color(0xFF00BCD4); // Cyan - tech/movement
  static const accent     = Color(0xFFFFB300); // Amber - African warmth

  // Status Colors
  static const success    = Color(0xFF4CAF50);
  static const warning    = Color(0xFFFF9800);
  static const danger     = Color(0xFFF44336);

  // Traffic Heatmap
  static const trafficFree = Color(0xFF4CAF50);
  static const trafficSlow = Color(0xFFFF9800);
  static const trafficJam  = Color(0xFFF44336);

  // Backgrounds
  static const background = Color(0xFFF5F7FA);
  static const surface    = Color(0xFFFFFFFF);
  static const darkBg     = Color(0xFF0A0E1A);

  // Text
  static const textDark   = Color(0xFF1A1A2E);
  static const textLight  = Color(0xFFFFFFFF);
  static const textGrey   = Color(0xFF757575);
}
